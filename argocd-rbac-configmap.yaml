apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
  namespace: argocd
data:
  kustomize.buildOptions: --enable-helm
  policy.default: role:none
  policy.csv: |
    p, role:none, *, *, */*, deny
    g, argocd-admin, role:admin
  scopes: '[groups]'